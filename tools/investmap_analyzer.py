# ╔══════════════════════════════════════════════════════════════╗
# ║             tools/investmap_analyzer.py                     ║
# ║  Оценка заполняемости карточки инвестплощадки               ║
# ║  Методика: Минэкономразвития РФ, 08.08.2023 №28301-МК/Д28и  ║
# ║  + публичный светофор invest.gov.ru                         ║
# ║  Версия алгоритма: 1.5.5                                    ║
# ╚══════════════════════════════════════════════════════════════╝

from __future__ import annotations
import re

PORTAL_FIELD_GAIN = 4  # ориентировочный прирост % за одно поле для текста уведомления

# ---------------------------------------------------------------------------
# БЛОК 1. СВЕДЕНИЯ ОБ ОБЪЕКТЕ (вес 15%)
# ---------------------------------------------------------------------------
BLOCK1_FIELDS = [
    ('Название площадки',                           'Название площадки',                True,  6),
    ('Преференциальный режим',                      'Преференциальный режим',           True,  5),
    ('Объект инфраструктуры поддержки',             'Объект инфраструктуры поддержки',  True,  5),
    ('Регион',                                      'Регион',                           True,  4),
    ('Муниципальное образование',                   'Муниципальное образование',        True,  4),
    ('Адрес объекта',                               'Адрес объекта',                    True,  4),
    ('Ближайший город',                             'Ближайший город',                  True,  3),
    ('Формат площадки',                             'Формат площадки',                  True,  5),
    ('Тип площадки',                                'Тип площадки (браун/гринфилд)',    True,  4),
    ('Фотографии',                                  'Фотографии объекта',               True,  8),
    ('Документы по объекту',                        'Документы по объекту',             False, 5),
    ('Координаты',                                  'Геопривязка',                      True,  10),
]
BLOCK1_MAX = 63
BLOCK1_WEIGHT = 0.15

# ---------------------------------------------------------------------------
# БЛОК 2. СВОБОДНЫЕ ПЛОЩАДИ И КОММЕРЧЕСКИЕ УСЛОВИЯ (вес 20%)
# ---------------------------------------------------------------------------
BLOCK2_FIELDS = [
    ('Форма собственности',                         'Форма собственности',              True,  5),
    ('Форма сделки',                                'Форма сделки',                     True,  6),
    ('Стоимость объекта',                           'Стоимость объекта, руб.',          True,  8),
    ('Стоимость, руб./год за га',                   'Стоимость, руб./год за га',        True,  6),
    ('Стоимость, руб./год за кв.м',                 'Стоимость, руб./год за кв.м',      True,  6),
    ('Сроки аренды',                                'Сроки аренды',                     True,  5),
    ('Порядок определения стоимости',               'Порядок определения стоимости',    True,  5),
    ('Класс опасности',                             'Класс опасности',                  False, 4),
    ('Характеристики ОКС',                          'Характеристики ОКС',               False, 5),
    ('Свободная площадь ЗУ',                        'Свободная площадь ЗУ, га',         True,  8),
    ('Кадастровый номер ЗУ',                        'Кадастровый номер ЗУ',             True,  5),
    ('Варианты разрешенного использования',         'ВРИ',                              True,  5),
    ('Межевание',                                   'Межевание',                        True,  4),
    ('Категория земель',                            'Категория земель',                 True,  4),
    ('Свободная площадь здания',                    'Свободная площадь здания, кв.м',   True,  8),
    ('Кадастровый номер здания',                    'Кадастровый номер здания',         True,  5),
    ('Технические характеристики здания',           'Тех. характеристики здания',       True,  5),
]
BLOCK2_MAX = 76
BLOCK2_WEIGHT = 0.20

BLOCK2_ZU_KEYS = {
    'Свободная площадь ЗУ', 'Кадастровый номер ЗУ',
    'Варианты разрешенного использования', 'Межевание', 'Категория земель',
}
BLOCK2_BUILDING_KEYS = {
    'Свободная площадь здания', 'Кадастровый номер здания', 'Технические характеристики здания',
}

# ---------------------------------------------------------------------------
# БЛОК 3. ТЕХНИЧЕСКОЕ ПРИСОЕДИНЕНИЕ (вес 30%)
# ---------------------------------------------------------------------------
BLOCK3_RESOURCES = [
    ('Водоснабжение Наличие', [
        ('Водоснабжение Наличие',                      4),
        ('Водоснабжение Тариф на питьевую воду',       3),
        ('Водоснабжение Тариф на транспортировку',     3),
        ('Объекты водоснабжения Максимально допустимая мощность', 3),
        ('Объекты водоснабжения Свободная мощность',   3),
        ('Водоснабжение Плата за подключение',         2),
        ('Объекты водоснабжения Иные характеристики',  2),
    ]),
    ('Водоотведение Наличие', [
        ('Водоотведение Наличие',                      4),
        ('Водоотведение Тариф на водотведение',        3),
        ('Водоотведение Плата за подключение',         2),
        ('Объекты водоотведения Иные характеристики',  2),
    ]),
    ('Газоснабжение Наличие', [
        ('Газоснабжение Наличие',                      4),
        ('Газоснабжение Тариф на потребление',         3),
        ('Газоснабжение Тариф на услуги по транспортировке', 3),
        ('Объекты газоснабжения Величина максимального расхода', 3),
        ('Объекты газоснабжения Свободная мощность',   3),
        ('Газоснабжение Плата за подключение',         2),
        ('Объекты газоснабжения Иные характеристики',  2),
    ]),
    ('Электроснабжение Наличие', [
        ('Электроснабжение Наличие',                   4),
        ('Электроснабжение Тариф на потребление',      3),
        ('Объекты электроснабжения Тариф на услуги по передаче', 3),
        ('Объекты электроснабжения Максимальная мощность', 3),
        ('Объекты электроснабжения Свободная мощность', 3),
        ('Электроснабжение Плата за подключение',      2),
        ('Объекты электроснабжения Иные характеристики', 2),
    ]),
    ('Теплоснабжение Наличие', [
        ('Теплоснабжение Наличие',                     4),
        ('Теплоснабжение Тариф на потребление',        3),
        ('Теплоснабжение Тариф на услуги по передаче', 3),
        ('Теплоснабжение Максимальная мощность',       3),
        ('Теплоснабжение Свободная мощность',          3),
        ('Теплоснабжение Плата за подключение',        2),
        ('Объекты теплоснабжения Иные характеристики', 2),
    ]),
    ('Вывоз ТКО Наличие', [
        ('Вывоз ТКО Наличие',                          4),
        ('Тариф на вывоз ТКО',                         3),
        ('Объекты ТКО Иные характеристики',            2),
    ]),
]
BLOCK3_MAX = 100
BLOCK3_WEIGHT = 0.30

# ---------------------------------------------------------------------------
# БЛОК 4. ТРАНСПОРТНАЯ ДОСТУПНОСТЬ (вес 15%)
# ---------------------------------------------------------------------------
BLOCK4_FIELDS = [
    ('Наличие подъездных путей',        'Наличие подъездных путей',               True,  15),
    ('Наличие ж/д',                     'Наличие ж/д путей',                      True,  15),
    ('Наличие парковки',                'Наличие парковки грузового транспорта',  True,  10),
    ('Транспортная доступность',        'Иные характеристики транспорта',         False, 10),
]
BLOCK4_MAX = 50
BLOCK4_WEIGHT = 0.15

# ---------------------------------------------------------------------------
# БЛОК 5. КОНТАКТЫ И ИНВЕСТ. ПРИВЛЕКАТЕЛЬНОСТЬ (вес 20%)
# ---------------------------------------------------------------------------
BLOCK5_FIELDS = [
    ('Наименование собственника',               'Наименование собственника',                    True,  6),
    ('ИНН собственника',                        'ИНН собственника',                             False, 3),
    ('Контактное лицо',                         'Контактное лицо',                              False, 4),
    ('Телефон',                                 'Телефон / e-mail',                             True,  8),
    ('Сайт',                                    'Сайт',                                         False, 4),
    ('Примечание',                              'Примечание',                                   False, 3),
    ('Описание процедуры подачи заявки',        'Описание процедуры подачи заявки',             True,  8),
    ('Перечень документов',                     'Перечень документов для заявки',               False, 5),
    ('Ссылка на форму подачи заявки',           'Ссылка на форму подачи заявки',                False, 4),
    ('Адрес эл. почты для подачи заявки',       'Email для подачи заявки',                      True,  7),
    ('Перечень видов экономической',            'Перечень ОКВЭД',                               True,  6),
    ('Градостроительные характеристики',        'Градостроительные характеристики',             False, 4),
    ('Документы территориального планирования', 'Документы территориального планирования',      True,  6),
    ('Иные сведения',                           'Иные сведения',                                False, 4),
    ('Фотографии',                              'Фотографии (блок 5)',                          False, 5),
    ('Наличие обременений',                     'Обременения',                                  True,  5),
]
BLOCK5_MAX = 82
BLOCK5_WEIGHT = 0.20

PORTAL_GREEN = 75
PORTAL_YELLOW = 60

# ---------------------------------------------------------------------------
# PORTAL_FIELDS — официальный список полей портала invest.gov.ru
# v1.5.1: преф/инфра поля всегда в знаменателе
# v1.5.2: ОКВЭД 'Нет ограничений' не засчитывается
# v1.5.3: 'не применимо' в Процедуре/Документах не засчитывается никогда
# v1.5.4: strip HTML-тегов из значений перед любой проверкой (_find/_strip_html)
# v1.5.5: «Нет» в полях «Наличие ж/д» и «Наличие парковки» не засчитывается
#         (портал ожидает содержательное значение: Да / Возможно создание)
# ---------------------------------------------------------------------------
PORTAL_FIELDS = [
    ('Статус',                                              None),
    ('Преференциальный режим',                              None),
    ('Наименование объекта преференциального режима',       None),
    ('Объект инфраструктуры поддержки',                     None),
    ('Наименование объекта инфраструктуры поддержки',       None),
    ('Регион',                                              None),
    ('Муниципальное образование',                           None),
    ('Адрес объекта',                                       None),
    ('Ближайший город',                                     None),
    ('Формат площадки',                                     None),
    ('Тип площадки',                                        None),
    ('Форма собственности',                                 None),
    ('Форма сделки',                                        None),
    ('Стоимость объекта',                                   None),
    ('Сроки аренды',                                        None),
    ('Порядок определения стоимости',                       None),
    ('Свободная площадь ЗУ',                                'ЗУ'),
    ('Свободная площадь здания',                            'здание'),
    ('Варианты разрешенного использования',                 'ЗУ'),
    ('Межевание',                                           'ЗУ'),
    ('Категория земель',                                    'ЗУ'),
    ('Кадастровый номер ЗУ',                                'ЗУ'),
    ('Кадастровый номер здания',                            'здание'),
    ('Технические характеристики здания',                   'здание'),
    ('Наименование собственника',                           None),
    ('Водоснабжение Наличие',                               None),
    ('Наличие подъездных путей',                            None),
    ('Наличие ж/д',                                         None),
    ('Наличие парковки',                                    None),
    ('Описание процедуры подачи заявки',                    None),
    ('Перечень документов',                                 None),
    ('Перечень видов экономической',                        None),
]

# ---------------------------------------------------------------------------
# v1.5.2+: нерелевантные значения для конкретных полей портала.
# v1.5.3: добавлены 'описание процедуры' и 'перечень документов' —
#         'не применимо' и аналоги НЕ засчитываются НИКОГДА
# v1.5.4: _find() теперь применяет _strip_html() к значению перед возвратом
# v1.5.5: добавлены 'наличие ж/д' и 'наличие парковки' — значение 'нет'
#         НЕ засчитывается (портал ожидает Да / Возможно создание)
# ---------------------------------------------------------------------------
IRRELEVANT_FIELD_VALUES: dict[str, set[str]] = {
    'перечень видов экономической': {
        'нет ограничений',
        'без ограничений',
        'не ограничено',
        'любые',
        'нет',
    },
    'описание процедуры': {
        'не применимо',
        'не применимо.',
        'не применимо ',
        'нет',
        'н/д',
        'не применяется',
        'не применима',
    },
    'перечень документов': {
        'не применимо',
        'не применимо.',
        'не применимо ',
        'нет',
        'н/д',
        'не применяется',
        'не применима',
    },
    # v1.5.5: портал не засчитывает «Нет» как содержательный ответ
    # для этих полей — ожидается «Да» или «Возможно создание»
    'наличие ж/д': {
        'нет',
    },
    'наличие парковки': {
        'нет',
    },
}

# Словарь для проверки опечаток в ключевых полях
TYPO_CHECKS = {
    'Порядок определения стоимости': {
        'отсутсвует': 'отсутствует',
        'отсутсвует ': 'отсутствует',
    },
    'min и max сроки аренды': {
        'не примениом': 'не применимо',
        'не применима': 'не применимо',
    },
    'Описание процедуры подачи заявки': {
        'не применима': 'не применимо',
    },
    'Перечень документов': {
        'не применима': 'не применимо',
    },
}

# ---------------------------------------------------------------------------
# Инструкции для каждого поля в тексте уведомления
# ---------------------------------------------------------------------------
FIELD_INSTRUCTIONS = {
    'Стоимость объекта': 'укажите стоимость аренды или покупки в рублях',
    'Стоимость, руб./год за га': 'укажите стоимость в руб./год за га',
    'Стоимость, руб./год за кв.м': 'укажите стоимость в руб./год за кв.м',
    'Наименование объекта преференциального режима': 'выберите значение из выпадающего списка на портале',
    'Наименование объекта инфраструктуры поддержки': 'выберите значение из выпадающего списка на портале',
    'Наличие ж/д': 'укажите корректное значение (Да / Нет / Возможно создание)',
    'Наличие парковки': 'укажите корректное значение (Да / Нет / Возможно создание)',
    'Свободная площадь ЗУ': 'укажите свободную площадь земельного участка в гектарах',
    'Свободная площадь здания': 'укажите свободную площадь здания в кв. м',
    'Кадастровый номер ЗУ': 'укажите кадастровый номер земельного участка',
    'Кадастровый номер здания': 'укажите кадастровый номер здания / сооружения',
    'Технические характеристики здания': 'добавьте технические характеристики здания',
    'Варианты разрешенного использования': 'укажите варианты разрешённого использования участка',
    'Межевание': 'укажите статус межевания земельного участка',
    'Категория земель': 'укажите категорию земель',
    'Сроки аренды': 'укажите минимальный и максимальный срок аренды',
    'Порядок определения стоимости': 'опишите порядок определения стоимости объекта',
    'Описание процедуры подачи заявки': 'добавьте описание процедуры подачи заявки инвестором',
    'Перечень документов': 'добавьте перечень документов, необходимых для подачи заявки',
    'Перечень видов экономической': 'укажите конкретные виды экономической деятельности (коды ОКВЭД или наименования отраслей)',
    'Водоснабжение Наличие': 'заполните сведения о наличии коммуникаций',
    'Наличие подъездных путей': 'укажите наличие подъездных путей к объекту',
    'Наименование собственника': 'укажите наименование собственника или администратора объекта',
    'Форма собственности': 'укажите форму собственности объекта',
    'Форма сделки': 'укажите форму сделки (аренда / продажа)',
}

# ---------------------------------------------------------------------------
# Значения родительских полей, при которых зависимые поля недоступны на портале
# ---------------------------------------------------------------------------
PREF_INACTIVE_VALUES = {'отсутствует', 'пусто', ''}
INFRA_INACTIVE_VALUES = {'без льгот', 'пусто', ''}


def _norm(s: str) -> str:
    """Нормализация строки для поиска: нижний регистр + замена ё→е."""
    return s.lower().replace('ё', 'е')


def _strip_html(s: str) -> str:
    """
    v1.5.4: Удаляет HTML-теги из строки.
    Пример: '<p>Не применимо.</p>' → 'Не применимо.'
    Применяется в _find() перед возвратом значения.
    """
    return re.sub(r'<[^>]+>', '', s).strip()


def _find(data: dict, key: str) -> str:
    key_n = _norm(key)
    for k, v in data.items():
        if key_n in _norm(k):
            # v1.5.4: очищаем HTML-теги из значения перед возвратом
            return _strip_html(str(v).strip()) if v is not None else 'ПУСТО'
    return 'ПУСТО'


def _find_exact(data: dict, key: str) -> str:
    key_n = _norm(key)
    for k, v in data.items():
        if _norm(k) == key_n:
            # v1.5.4: очищаем HTML-теги из значения перед возвратом
            return _strip_html(str(v).strip()) if v is not None else 'ПУСТО'
    return 'ПУСТО'


def _is_filled(val: str) -> bool:
    return val not in ('ПУСТО', '', None)


def _check_typos(data: dict) -> list[str]:
    """
    Проверяет ключевые поля на опечатки.
    Возвращает список строк-предупреждений вида:
      «Поле X: найдено «опечатка» — возможно, имелось в виду «правильно».»
    """
    warnings = []
    for field_key, typo_map in TYPO_CHECKS.items():
        val = _find(data, field_key)
        if not _is_filled(val):
            continue
        val_stripped = val.strip()
        val_lower = val_stripped.lower()
        for typo, correct in typo_map.items():
            if val_lower == typo.lower():
                warnings.append(
                    f'«{field_key}»: найдено «{val_stripped}» — возможно, имелось в виду «{correct}».'
                )
                break
    return warnings


def _is_irrelevant(key: str, val: str) -> bool:
    """
    v1.5.2+: проверяет, является ли значение поля нерелевантным для портала.
    v1.5.3: охватывает ОКВЭД, Процедуру подачи заявки и Перечень документов.
    v1.5.4: val уже очищен от HTML через _find/_strip_html.
    v1.5.5: охватывает «Наличие ж/д» и «Наличие парковки» — «Нет» не засчитывается.
    Поиск по частичному совпадению ключа (нижний регистр).
    """
    key_n = _norm(key)
    for field_fragment, bad_values in IRRELEVANT_FIELD_VALUES.items():
        if field_fragment in key_n:
            return _norm(val).strip() in bad_values
    return False


def _is_filled_portal(val: str, key: str, data: dict) -> bool:
    """
    Расширенная проверка заполненности для портального счётчика.

    Правила:
    1. «Порядок определения стоимости» с любым непустым значением → засчитывается.
    2. v1.5.2+: поля из IRRELEVANT_FIELD_VALUES с нерелевантным значением
       → НЕ засчитываются (портал ожидает содержательный ответ).
       v1.5.3: 'не применимо' в Процедуре/Документах НЕ засчитывается НИКОГДА.
       v1.5.4: val уже очищен от HTML через _find().
       v1.5.5: «Нет» в «Наличие ж/д» и «Наличие парковки» НЕ засчитывается.
    3. Во всех остальных случаях — стандартный _is_filled.
    """
    if not _is_filled(val):
        return False

    if key == 'Порядок определения стоимости':
        return True

    # v1.5.2+: нерелевантные значения не засчитываются
    if _is_irrelevant(key, val):
        return False

    return True


def _presence_factor(val: str) -> float:
    if not _is_filled(val):
        return 0.0
    v = val.lower()
    if 'да' in v:
        return 1.0
    if 'возможно' in v:
        return 0.5
    if 'нет' in v:
        return 0.5
    return 1.0


def _get_format(data: dict) -> str:
    val = _find(data, 'Формат площадки').lower()
    if 'земельн' in val or 'участок' in val or 'зу' in val:
        return 'ЗУ'
    if 'здание' in val or 'сооружение' in val or 'помещение' in val:
        return 'здание'
    return 'неизвестно'


def _get_status(data: dict) -> str:
    val = _find(data, 'Статус').lower()
    if 'своб' in val:
        return 'свободна'
    if 'реализ' in val or 'снята' in val:
        return 'реализована'
    return 'иное'


def _get_id(data: dict) -> str:
    val = _find_exact(data, 'global_id')
    if _is_filled(val):
        return str(val)
    for key in ('Название площадки', 'Код во внешнем источнике'):
        val = _find(data, key)
        if _is_filled(val):
            return str(val)[:60]
    return 'ID не определён'


def _get_name(data: dict, площадка_id: str) -> str:
    for key in ('Название площадки', 'Наименование площадки', 'Название объекта'):
        val = _find(data, key)
        if _is_filled(val):
            return str(val)
    return f'площадке {площадка_id}'


def _instruction_for_field(field: str) -> str:
    if field in FIELD_INSTRUCTIONS:
        return FIELD_INSTRUCTIONS[field]
    for key, instruction in FIELD_INSTRUCTIONS.items():
        if key.lower() in field.lower() or field.lower() in key.lower():
            return instruction
    return 'заполните поле'


def _pluralize_field(n: int) -> str:
    """Склонение слова 'поле' по числу."""
    if 11 <= n % 100 <= 19:
        return 'полей'
    r = n % 10
    if r == 1:
        return 'поле'
    if 2 <= r <= 4:
        return 'поля'
    return 'полей'


def _build_sms(площадка_id: str, площадка_name: str, total_portal: int,
               missing_portal: list[str], typo_warnings: list[str] | None = None) -> str | None:
    """Формирует текст уведомления для копирования в Telegram."""
    if not missing_portal and not typo_warnings:
        return None

    lines = ['Добрый день!', '']

    if missing_portal:
        count = len(missing_portal)
        word = _pluralize_field(count)
        lines.append(
            f'По площадке «{площадка_name}» (ID: {площадка_id}) '
            f'заполняемость на портале invest.gov.ru составляет {total_portal}%.'
        )
        lines.append(
            f'Для повышения показателя просим заполнить {count} {word} — '
            f'каждое даёт ориентировочно +{PORTAL_FIELD_GAIN}%:'
        )
        lines.append('')
        for i, field in enumerate(missing_portal, 1):
            lines.append(f'{i}. {field} — {_instruction_for_field(field)}.')

    if typo_warnings:
        lines.append('')
        lines.append('⚠️ Обнаружены возможные опечатки в данных:')
        for w in typo_warnings:
            lines.append(f'   • {w}')
        lines.append('   Рекомендуем проверить и исправить.')

    lines.append('')
    lines.append('После внесения изменений просим сообщить — проверим обновлённый процент.')
    return '\n'.join(lines)


def build_summary_sms(analysis_list: list[dict]) -> str | None:
    """Сводная SMS по всем площадкам из формата 2."""
    items = [a for a in analysis_list if a.get('sms')]
    if not items:
        return None

    total = len(analysis_list)
    problem = len(items)

    lines = ['Добрый день!', '']
    lines.append(
        f'Направляем сводный отчёт по заполняемости инвестплощадок на портале invest.gov.ru.'
    )
    lines.append(
        f'Проверено площадок: {total}. '
        f'Требуют доработки: {problem}.'
    )
    lines.append('')

    for a in items:
        lines.append(f'▸ «{a["id"]}» — {a["total"]}% ({a["category"]})')
        missing = a.get('missing_portal') or []
        if missing:
            for i, field in enumerate(missing, 1):
                lines.append(f'   {i}. {field} — {_instruction_for_field(field)}.')
        typos = a.get('typo_warnings') or []
        if typos:
            lines.append('   ⚠️ Опечатки:')
            for w in typos:
                lines.append(f'      • {w}')
        lines.append('')

    lines.append('После внесения изменений просим сообщить — проверим обновлённый процент.')
    return '\n'.join(lines)


def _score_portal(data: dict, fmt: str) -> tuple[int, list[str]]:
    filled_count = 0
    total_count = 0
    missing = []

    for key, only_fmt in PORTAL_FIELDS:
        if only_fmt is not None and only_fmt != fmt:
            continue

        total_count += 1

        if key == 'Водоснабжение Наличие':
            comm_keys = [
                'Водоснабжение Наличие',
                'Водоотведение Наличие',
                'Газоснабжение Наличие',
                'Теплоснабжение Наличие',
                'Электроснабжение Наличие',
            ]
            filled = any(_is_filled(_find(data, k)) for k in comm_keys)
        else:
            val = _find(data, key)
            filled = _is_filled_portal(val, key, data)

        if filled:
            filled_count += 1
        else:
            missing.append(key)

    score = round(filled_count / max(total_count, 1) * 100)
    return score, missing


def _score_block1(data: dict) -> tuple[float, list[str]]:
    earned = 0
    missing = []
    for key, label, required, points in BLOCK1_FIELDS:
        if key == 'Координаты':
            geo_val = (
                _find(data, 'Координаты (полигон)') +
                _find(data, 'Координаты (точка)') +
                _find(data, 'Координаты (линия)')
            ).replace('ПУСТО', '').strip()
            filled = bool(geo_val)
        else:
            filled = _is_filled(_find(data, key))
        if filled:
            earned += points
        elif required:
            missing.append(label)
    score = round(earned / BLOCK1_MAX * 100)
    return score, missing


def _score_block2(data: dict, fmt: str) -> tuple[float, list[str]]:
    earned = 0
    missing = []
    is_zu = fmt == 'ЗУ'
    is_building = fmt == 'здание'
    for key, label, required, points in BLOCK2_FIELDS:
        if key in BLOCK2_ZU_KEYS and not is_zu:
            continue
        if key in BLOCK2_BUILDING_KEYS and not is_building:
            continue
        filled = _is_filled(_find(data, key))
        if filled:
            earned += points
        elif required:
            missing.append(label)
    active_max = sum(
        p for k, l, r, p in BLOCK2_FIELDS
        if not (k in BLOCK2_ZU_KEYS and not is_zu)
        and not (k in BLOCK2_BUILDING_KEYS and not is_building)
    )
    score = round(earned / max(active_max, 1) * 100)
    return score, missing


def _score_block3(data: dict) -> tuple[float, list[str]]:
    earned = 0
    missing = []
    for presence_key, sub_fields in BLOCK3_RESOURCES:
        presence_val = _find(data, presence_key)
        factor = _presence_factor(presence_val)
        for field_key, pts in sub_fields:
            if field_key == presence_key:
                earned += pts * factor
                if factor == 0:
                    missing.append(field_key)
            else:
                val = _find(data, field_key)
                if _is_filled(val):
                    earned += pts
                elif factor == 1.0:
                    missing.append(field_key)
    score = round(earned / BLOCK3_MAX * 100)
    return score, missing


def _score_block4(data: dict) -> tuple[float, list[str]]:
    earned = 0
    missing = []
    for key, label, required, points in BLOCK4_FIELDS:
        if _is_filled(_find(data, key)):
            earned += points
        elif required:
            missing.append(label)
    score = round(earned / BLOCK4_MAX * 100)
    return score, missing


def _score_block5(data: dict) -> tuple[float, list[str]]:
    earned = 0
    missing = []
    for key, label, required, points in BLOCK5_FIELDS:
        if _is_filled(_find(data, key)):
            earned += points
        elif required:
            missing.append(label)
    score = round(earned / BLOCK5_MAX * 100)
    return score, missing


def analyze(data: dict) -> dict:
    fmt = _get_format(data)
    status = _get_status(data)
    площадка_id = _get_id(data)
    площадка_name = _get_name(data, площадка_id)
    signed = _find(data, 'Состояние подписи')

    b1, m1 = _score_block1(data)
    b2, m2 = _score_block2(data, fmt)
    b3, m3 = _score_block3(data)
    b4, m4 = _score_block4(data)
    b5, m5 = _score_block5(data)

    total_mer = round(
        b1 * BLOCK1_WEIGHT +
        b2 * BLOCK2_WEIGHT +
        b3 * BLOCK3_WEIGHT +
        b4 * BLOCK4_WEIGHT +
        b5 * BLOCK5_WEIGHT
    )

    total_portal, missing_portal = _score_portal(data, fmt)
    typo_warnings = _check_typos(data)

    if total_portal >= PORTAL_GREEN:
        category = '🟢 Готова к публикации'
        ready = 'да'
    elif total_portal >= PORTAL_YELLOW:
        category = '🟡 Требует доработки'
        ready = 'после доработки'
    else:
        category = '🟠 Значительные пробелы'
        ready = 'нет'

    all_missing = m1 + m2 + m3 + m4 + m5
    sms = _build_sms(площадка_id, площадка_name, total_portal, missing_portal, typo_warnings)

    return {
        'id': площадка_id,
        'format': fmt,
        'status': status,
        'blocks': {
            1: {'score': b1, 'weight': BLOCK1_WEIGHT, 'contribution': round(b1 * BLOCK1_WEIGHT), 'missing': m1},
            2: {'score': b2, 'weight': BLOCK2_WEIGHT, 'contribution': round(b2 * BLOCK2_WEIGHT), 'missing': m2},
            3: {'score': b3, 'weight': BLOCK3_WEIGHT, 'contribution': round(b3 * BLOCK3_WEIGHT), 'missing': m3},
            4: {'score': b4, 'weight': BLOCK4_WEIGHT, 'contribution': round(b4 * BLOCK4_WEIGHT), 'missing': m4},
            5: {'score': b5, 'weight': BLOCK5_WEIGHT, 'contribution': round(b5 * BLOCK5_WEIGHT), 'missing': m5},
        },
        'total': total_portal,
        'total_portal': total_portal,
        'total_mer': total_mer,
        'category': category,
        'ready': ready,
        'all_missing': all_missing,
        'missing_portal': missing_portal,
        'typo_warnings': typo_warnings,
        'sms': sms,
        'signed': signed,
    }
