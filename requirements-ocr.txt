# Опциональные зависимости для OCR-распознавания анкет (сканы, изображения)
# Устанавливается отдельно: pip install -r requirements-ocr.txt
#
# Внимание: тянет PyTorch (~500 МБ) — не нужен организациям без OCR-анкет
-r requirements.txt
easyocr
